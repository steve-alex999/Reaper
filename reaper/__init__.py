"""Reaper - a CLI that reaps job descriptions into tailored applications.

Paste a job-application link; Reaper fetches the JD, pre-screens it, runs the
locked three-prompt workflow to build a custom resume, assembles a ready-to-paste
answer pack, and (on supported ATS platforms) drives a browser to fill the form.
"""

__version__ = "0.1.0"
